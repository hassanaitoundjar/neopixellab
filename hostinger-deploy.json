{
  "name": "neo-pixel-lab",
  "version": "1.0.0",
  "description": "Neo Pixel Lab - Free Image Converter Tool",
  "main": "index.html",
  "scripts": {
    "start": "node -e \"console.log('Static site - no server needed')\"",
    "build": "npm install && npm run build:vite && cp -r dist/* ./",
    "build:vite": "vite build",
    "install": "npm install",
    "deploy": "bash deploy.sh"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/hassanaitoundjar/neopixellab.git"
  },
  "keywords": [
    "image-converter",
    "react",
    "vite",
    "typescript",
    "tailwindcss"
  ],
  "author": "Hassan Ait Oundjar",
  "license": "MIT",
  "buildCommand": "npm install && npm run build:vite && cp -r dist/* ./",
  "outputDirectory": "./",
  "framework": "vite"
}
